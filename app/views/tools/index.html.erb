<h1>Listing tools</h1>

<% @tools.each do |tool| %>
  <div>
    <h3><%= tool.detail %></h3>
    <p>Price : <%= tool.price %></p>
    <%= link_to "Show", tool_path(tool) %>
  </div>
<% end %>

<%= link_to "New tool", new_tool_path %>
