<h1>New Tool</h1>

<%= form_with(model: @tool, url: tools_path, local: true) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :detail %>
    <%= f.text_field :detail %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>

  <div class="actions">
    <%= f.submit "Create Tool" %>
  </div>
<% end %>

<%= link_to 'Back', tools_path %>
