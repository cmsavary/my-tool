<h1>Détail of tool</h1>

<p><strong>Name:</strong> <%= @tool.name %></p>
<p><strong>Detail:</strong> <%= @tool.detail %></p>
<p><strong>Price:</strong> <%= @tool.price %></p>



<%= link_to 'Reserve this Tool', new_tool_booking_path(@tool), class: 'button' %>


<% @tool.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>


<%= link_to "Back", tools_path %>
